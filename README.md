# SVG-border-shape
Paste any SVG frame and get the matching border-shape path plus two shape-outside polygons that make text flow along the contour. Works in Chrome 147+ (Canary 146+ behind the experimental web-platform features flag).
